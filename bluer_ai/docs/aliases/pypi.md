# aliases: pypi

```bash
@pypi \
	browse \
	[plugin=<plugin-name>,token]
 . browse pypi/bluer_ai.
@pypi \
	build \
	[browse,install,~rm_dist,~upload,plugin=<plugin-name>]
 . build pypi/bluer_ai.
@pypi \
	install
 . install pypi.
```
