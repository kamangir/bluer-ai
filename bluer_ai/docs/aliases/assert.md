# assert

```bash
@assert \
	<this> \
	<that> \
	[yes | no | empty | non-empty]
 . assert <this> <?> <that>.
@assert \
	list \
	<this,that,which> \
	<that,this,what>
 . assert that the two lists are identical.
```
